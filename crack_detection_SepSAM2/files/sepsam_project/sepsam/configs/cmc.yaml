# ===== CMC 超參數（論文 Table 3）=====
YOLO_CONF_1: 0.0
YOLO_CONF_2: 0.5
SAM_THRESH: 0.85           # 換 SAM2 後務必重新校準（附錄 D.5）
CONFLICTION_RATIO: 1.50
POINTS_DIVISOR: 50         # n_points = max(H,W)//POINTS_DIVISOR（最佳約 60 點/影像）

agent_ckpt: runs/sepsam_agent/weights/best.pt
device: cuda

# ===== 大模型後端：'v1'（SAM ViT-H，對齊論文）或 'sam2'（改良）=====
sam_backend: v1

sam_model_type: vit_h
sam_ckpt: weights/sam_vit_h_4b8939.pth

sam2_cfg: configs/sam2.1/sam2.1_hiera_b+.yaml
sam2_ckpt: weights/sam2.1_hiera_base_plus.pt
